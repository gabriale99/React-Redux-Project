import React from 'react';
import MainContent from "../../MainContent"

function SearchField(props) {
    return (
        <MainContent content="Search Field section"/>
    );
}

export default SearchField;