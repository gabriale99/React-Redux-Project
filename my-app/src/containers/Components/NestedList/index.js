import React from 'react';
import MainContent from "../../MainContent"

function NestedList(props) {
    return (
        <MainContent content="Nested List section"/>
    );
}

export default NestedList;