import React from 'react';
import MainContent from "../../MainContent"

function StackedBar(props) {
    return (
        <MainContent content="Stacked Bar section"/>
    );
}

export default StackedBar;