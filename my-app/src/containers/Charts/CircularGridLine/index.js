import React from 'react';
import MainContent from "../../MainContent"

function CircularGridLine(props) {
    return (
        <MainContent content="Circula grid line section"/>
    );
}

export default CircularGridLine;